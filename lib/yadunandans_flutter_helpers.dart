library yadunandans_flutter_helpers;

import 'package:flutter/material.dart';

/* class Scaffold2 extends StatefulWidget {
  final Widget? body;
  final PreferredSizeWidget? appBar;
  Scaffold2({this.body, this.appBar, Key? key}) : super(key: key);

  @override
  _Scaffold2State createState() => _Scaffold2State();

  _Scaffold2State 
}

class _Scaffold2State extends State<Scaffold2> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Stack(
        children: [
          if (widget.body is Widget) widget.body!,
          IgnorePointer(),
        ],
      ),
    );
  }
} */

export 'themed_app.dart';
